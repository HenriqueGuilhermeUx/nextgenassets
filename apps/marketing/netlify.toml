[build]
  base    = "."
  publish = "apps/marketing/out"
  command = "cd apps/marketing && NEXT_TELEMETRY_DISABLED=1 npm run build"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
  NEXT_TELEMETRY_DISABLED = "1"

# SEM @netlify/plugin-nextjs (usa static export, nao precisa!)
