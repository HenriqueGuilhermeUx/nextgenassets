[build]
  base    = "."
  publish = "apps/consumer/out"
  command = "cd apps/consumer && NEXT_TELEMETRY_DISABLED=1 npm run build"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
  NEXT_TELEMETRY_DISABLED = "1"

# SEM @netlify/plugin-nextjs (usa static export, nao precisa!)
